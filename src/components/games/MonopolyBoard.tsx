import { BOARD, calculateRent, ownsFullGroup, type MonopolyState, type PropertyColor } from "@/lib/monopoly-engine";
import { useState } from "react";

const COLOR_HEX: Record<PropertyColor, string> = { brown:"#8B4513","light-blue":"#87CEEB",pink:"#FF69B4",orange:"#FFA500",red:"#DC2626",yellow:"#FFD700",green:"#16A34A","dark-blue":"#1E3A8A" };
const TOKEN_COLORS=["#ef4444","#3b82f6","#22c55e","#eab308"];
function gridPos(id:number){ if(id===0)return{row:11,col:11}; if(id<=9)return{row:11,col:11-id}; if(id===10)return{row:11,col:1}; if(id<=19)return{row:11-(id-10),col:1}; if(id===20)return{row:1,col:1}; if(id<=29)return{row:1,col:1+(id-20)}; if(id===30)return{row:1,col:11}; return{row:1+(id-30),col:11}; }

export function MonopolyBoard({state,players,myPlayerId,myTurn,disabled,onRoll,onBuy,onPass,onPayBail,onUseJailCard,onBuildHouse,onMortgage,onUnmortgage,onEndTurn}:{state:MonopolyState;players:{id:string;name:string}[];myPlayerId:string;myTurn:boolean;disabled?:boolean;onRoll:()=>void;onBuy:()=>void;onPass:()=>void;onPayBail:()=>void;onUseJailCard:()=>void;onBuildHouse:(id:number)=>void;onMortgage:(id:number)=>void;onUnmortgage:(id:number)=>void;onEndTurn:()=>void}){
 const [selected,setSelected]=useState<number|null>(null); const colorIndex:Record<string,number>={}; players.forEach((p,i)=>colorIndex[p.id]=i);
 const me=state.players[myPlayerId]; const current=state.players[state.order[state.turnIndex]!]; const currentSpace=current?BOARD[current.position]:null;
 const mine=BOARD.filter(s=>state.properties[s.id]?.owner===myPlayerId);
 return <div className="grid w-full gap-4 xl:grid-cols-[280px_minmax(700px,1fr)_240px]">
  <aside className="order-2 rounded-2xl border bg-card p-4 shadow-sm xl:order-1"><h3 className="mb-3 text-lg font-bold">My properties</h3><div className="space-y-2">{mine.length?mine.map(s=>{const p=state.properties[s.id]!;return <button key={s.id} onClick={()=>setSelected(s.id)} className="w-full rounded-xl border p-3 text-left transition hover:scale-[1.01]" style={{borderLeft:`8px solid ${s.color?COLOR_HEX[s.color]:"#777"}`}}><div className="font-semibold">{s.name}</div><div className="text-xs text-muted-foreground">Rent: ${calculateRent(state,s.id)} · {p.houses===5?"Hotel":`${p.houses} houses`} {p.mortgaged?" · MORTGAGED":""}</div></button>}):<p className="text-sm text-muted-foreground">Buy properties to track rent and upgrades here.</p>}</div></aside>
  <main className="order-1 min-w-0 xl:order-2"><div className="mx-auto grid aspect-square w-full max-w-[900px] rounded-2xl bg-background p-1 shadow-2xl" style={{gridTemplateColumns:"repeat(11,1fr)",gridTemplateRows:"repeat(11,1fr)"}}>{BOARD.map(space=>{const pos=gridPos(space.id),prop=state.properties[space.id],tokens=players.filter(p=>state.players[p.id]?.position===space.id&&!state.players[p.id]?.bankrupt);return <button key={space.id} onClick={()=>setSelected(space.id)} style={{gridRow:pos.row,gridColumn:pos.col}} className="relative flex min-h-0 flex-col overflow-hidden border bg-card text-[9px] transition hover:z-10 hover:scale-105"><div className="h-3 w-full" style={{backgroundColor:space.color?COLOR_HEX[space.color]:"transparent"}}/><div className="flex flex-1 flex-col items-center justify-center p-1 text-center font-medium">{space.name}{space.price&&<span className="text-muted-foreground">${space.price}</span>}{prop?.owner&&<span className="mt-1 h-2 w-6 rounded" style={{backgroundColor:TOKEN_COLORS[colorIndex[prop.owner]]}}/>}{prop&&prop.houses>0&&<span>{prop.houses===5?"🏨":"🏠".repeat(prop.houses)}</span>}</div><div className="absolute bottom-1 right-1 flex gap-0.5">{tokens.map(p=><span key={p.id} className="h-3 w-3 animate-pulse rounded-full border-2 border-white" style={{backgroundColor:TOKEN_COLORS[colorIndex[p.id]]}}/>)}</div></button>})}<section style={{gridRow:"2 / 11",gridColumn:"2 / 11"}} className="flex flex-col items-center justify-center gap-4 rounded-xl border bg-gradient-to-br from-secondary/40 to-primary/10 p-6 text-center"><h1 className="text-3xl font-black tracking-wide">MONOPOLY</h1>{state.dice&&<div className="flex gap-3 text-4xl"><span className="animate-bounce rounded-xl bg-card p-3 shadow">{state.dice[0]}</span><span className="animate-bounce rounded-xl bg-card p-3 shadow">{state.dice[1]}</span></div>}{state.lastCard&&<div className="max-w-md rounded-xl border bg-card p-4 text-sm shadow">🃏 {state.lastCard}</div>}{myTurn&&!disabled&&<div className="flex flex-wrap justify-center gap-2">{state.phase==="pre-roll"&&<><button onClick={onRoll} className="rounded-xl bg-primary px-6 py-3 font-bold text-primary-foreground shadow hover:scale-105">{me?.inJail?"Roll for doubles":"🎲 Roll dice"}</button>{me?.inJail&&<button onClick={onPayBail} className="rounded-xl border px-4">Pay bail</button>}{me?.inJail&&me.jailFreeCards>0&&<button onClick={onUseJailCard} className="rounded-xl border px-4">Use Jail card</button>}</>}{state.phase==="awaiting-buy"&&currentSpace&&<><button onClick={onBuy} className="rounded-xl bg-primary px-5 py-3 font-bold text-primary-foreground">Buy {currentSpace.name} (${currentSpace.price})</button><button onClick={onPass} className="rounded-xl border px-5">Pass</button></>}{state.phase==="post-roll"&&<><button onClick={onEndTurn} className="rounded-xl bg-primary px-6 py-3 font-bold text-primary-foreground">End turn</button>{mine.filter(s=>s.color&&ownsFullGroup(state,myPlayerId,s.color)).map(s=><button key={s.id} onClick={()=>onBuildHouse(s.id)} className="rounded-xl border px-3 text-xs">🏠 Build {s.name}</button>)}</>}</div>}{!myTurn&&<p className="text-muted-foreground">Waiting for {players.find(p=>p.id===state.order[state.turnIndex])?.name}…</p>}</section></div></main>
  <aside className="order-3 rounded-2xl border bg-card p-4 shadow-sm"><h3 className="mb-3 font-bold">Players</h3><div className="space-y-2">{players.map((p,i)=>{const ps=state.players[p.id];return <div key={p.id} className={`rounded-xl border p-3 ${state.order[state.turnIndex]===p.id?"ring-2 ring-primary":""}`}><div className="flex items-center gap-2"><span className="h-3 w-3 rounded-full" style={{backgroundColor:TOKEN_COLORS[i]}}/><b>{p.name}</b></div><div className="mt-1 text-sm">💵 ${ps?.cash}</div></div>})}</div>{selected!==null&&<div className="mt-4 rounded-xl border p-3 text-sm"><b>{BOARD[selected]?.name}</b><div>Current rent: ${state.properties[selected]?.owner?calculateRent(state,selected):0}</div>{state.properties[selected]?.owner===myPlayerId&&<button className="mt-2 rounded-lg border px-2 py-1 text-xs" onClick={()=>state.properties[selected]?.mortgaged?onUnmortgage(selected):onMortgage(selected)}>{state.properties[selected]?.mortgaged?"Unmortgage":"Mortgage"}</button>}</div>}</aside>
 </div>;
}
